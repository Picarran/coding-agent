# Secrets
.env

# Exam paper (kept local, never committed)
推免考核题目学生版.pdf

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node / Vite (frontend)
node_modules/
web/frontend/dist/

# Compiled / agent-generated artifacts in the demo workspace
*.class

# Demo workspace: only the two seed files are tracked; everything the agent
# generates there is runtime scratch.
demo_workspace/*
!demo_workspace/calculator.py
!demo_workspace/test_calculator.py

# Eval artifacts (generated reports / audit logs / run history)
eval/reports/
eval/audit/
eval/runs/

# Agent runtime artifacts (raw command output, etc.)
.coding-agent/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
